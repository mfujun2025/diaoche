# 吊车.cn · P0 修复报告（SEO 基础文件 + 详情页免责声明）

**日期**：2026-09-21
**触发**：拆解竞品 `diaochehui.com` 时顺手查本站，发现两个硬伤
**状态**：已上线，CI run **#30 success**，提交 `2c5bca7`

---

## 一、修了什么

### 1. `robots.txt` 与 `sitemap.xml` 根本不存在 ⚠️

**发现过程**：拆竞品时对方 robots.txt 正常返回，我回头查自己站 ——

```
https://xn--bqr649k.cn/robots.txt   → 返回的是首页 HTML
https://xn--bqr649k.cn/sitemap.xml  → 返回的是首页 HTML
```

**不是 404，是被 Pages 的「SPA 式回落」吃掉了** —— 文件不存在时，Pages 会回落到首页。
所以这个漏洞**表面上完全看不出来**：浏览器打开不报错，只是拿到的内容不对。

**后果**：搜索引擎爬虫拿不到任何抓取指引，也不知道站点有哪些页面要收录。
对一个靠搜索流量吃饭的信息平台来说，这是最致命的一环。

**修法**：三份文件 + 一个动态接口

| 文件 | 内容 |
|---|---|
| `/robots.txt` | 允许抓取；禁 `/admin/`（后台）与 `/*?*`（筛选参数页是重复视图）；指向 sitemap |
| `/sitemap.xml` | sitemapindex，挂下面两个子 sitemap |
| `/pages-sitemap.xml` | 6 个静态页（首页 1.0 / 车源大厅 0.9 / 其余 0.7） |
| `/trucks-sitemap.xml` | **动态生成**：实时查库列出全部已审核车源详情页 |

**为什么车源 sitemap 必须动态**：
构建阶段拿不到 D1 数据（CI 里没有数据库凭据，也不该依赖），而车源是随时增删的 ——
每上一条新车源就重新构建一次不现实。所以改成 Function 实时查库，**新上架车源立即进 sitemap**。
缓存 10 分钟，兼顾新鲜度与回源压力。

### 2. 详情页缺免责声明

竞品（吊车汇）**每条车源详情页都写了免责声明**，我们只在 footer 写一份。

**为什么这有风险**：用户是从搜索引擎**直接落到某条车源页**的，未必会滚到底看 footer。
而纠纷恰恰最容易发生在这类页面上 —— 免责声明必须贴着车源信息出现。

**修法**：把声明从 footer 抽出来做成统一口径，同时用在两处。

| 文件 | 改动 |
|---|---|
| `src/site.mjs` | 抽出 `DISCLAIMER_SHORT`（footer 用）/ `DISCLAIMER_FULL`（详情页用） |
| `public/app.js` | 新增 `disclaimerHtml()`，渲染在「联系方式」下方 |
| `scripts/build.mjs` | `DETAIL_CSS` 新增 `.d-disclaimer` 样式 |

详情页声明含 4 条，重点是这三句：**不参与不担保实际交易**、**平台不做真实性背书**、**切勿先付款**。

---

## 二、踩的两个坑（值得记下来）

### 坑 1：Function 文件名带 `.` 会导致路由不生效

最初建的是 `functions/trucks-sitemap.xml.ts`，结果请求 `/trucks-sitemap.xml`
**返回 text/html** —— 说明 Function 完全没接管，请求漏到了静态资源层又回落到首页。

**改成目录形式才行**：`functions/trucks-sitemap.xml/index.ts`

### 坑 2：只导出 `onRequestGet` 时 HEAD 请求拿不到正确 Content-Type

改完目录形式后 GET 正常了，但 `curl -I`（HEAD）拿到的还是 `text/html`。

**原因**：只导出 `onRequestGet` 时，HEAD 请求不会被这个 handler 接住，漏到静态层。
部分爬虫会因此判定「这不是 XML」。

**修法**：改成统一在 `onRequest` 里处理 GET / HEAD 共用一份逻辑，
HEAD 时返回 `null` body（HTTP 规范要求 HEAD 不带响应体）。

---

## 三、验证结果

**本地（wrangler pages dev 8825）**

```
17 个路径全部 200，/trucks/99999/ 仍正确 404
4 个 SEO 文件 Content-Type 全部正确：
  robots.txt          text/plain
  sitemap.xml         application/xml
  pages-sitemap.xml   application/xml
  trucks-sitemap.xml  application/xml
后台接口回归：无密钥 401 / 对密钥 200
```

**CDP 无头浏览器实测详情页**（读 DOM，不靠肉眼）

```
title              = "25吨 徐工 QY25K5 转让 — 吊车.cn"   ✅
详情已渲染          = true
免责声明存在        = true
标题               = "免责声明"
条目数             = 4 条
视觉可见           = true（宽高均 > 0，非 display:none）
位于联系方式下方    = true
移动端 390 溢出     = false（scrollWidth 390 = clientWidth 390）
```

**线上（已验证生效）**

```
robots.txt           200  text/plain
sitemap.xml          200  application/xml
pages-sitemap.xml    200  application/xml
trucks-sitemap.xml   200  application/xml（列出 #2 #3 #4 三条真实车源）
全站 17 个路径全 200，/trucks/99999/ 404
www 域 3 个路径全 200（含 robots.txt）
```

---

## 四、下一步（P1，等你发话）

**把筛选参数页改成静态路径页**

现在 `?tonnage=25` 这种带查询参数的页面，搜索引擎收录意愿远低于静态路径页。
竞品主要靠「地区 × 分类」组合出几十个可收录页面吃长尾。

建议改成：

```
/trucks/25吨/          /trucks/徐工/          /trucks/江苏/
/trucks/25吨/江苏/     ...
```

**性价比高**：车源数据 D1 里现成就有（tonnage / brand / province 字段），
只需在 Functions 里加一层路由，**不用重新录任何数据**。
`吨位 9 种 × 地区 8 省 = 72 个组合页`，加上单独的吨位页、品牌页、地区页，
可以铺出 **100+ 个可收录页面**。

**另外还可以做**：首页补 JSON-LD（现在 0 处）和 og 标签（现在完全没有，分享到微信只有光秃秃的链接）。
