node_modules/
dist/
.wrangler/
.dev.vars
*.log
.DS_Store

# 本地无头浏览器验证的临时产物（截图、Edge profile）
.shots/

# 提交信息临时文件（git commit -F 用，避免 bash heredoc 在本机 Git Bash 下取不到路径）
commit-msg.tmp

# Pages 项目配置备份（排查环境变量问题时导出，可能含密钥等敏感信息）
.pages-config-backup.json
.probe-build/

# Pages 项目配置备份 / 本地构建探针
.pages-config-backup.json
.probe-build/

# 每日文章机器人：执行日志 + dir 目标的本地产物 + 自测临时目录
logs/
out/
.tmp-bot-test/

# 人工投稿稿件的暂存目录（--from-file 用，不进仓库）
manuscripts/
config/local.secrets.json
