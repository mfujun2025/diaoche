# 交付报告：指南文章栏目

**日期**：2026-09-21
**状态**：✅ 已上线，CI 全绿（11 步），线上验收 8 项通过
**提交**：本地 `7f2e22f` · `a168183`｜远端 `ff19d28` · `a0567df`

---

## 一句话

你的站原来**没有放文章的地方**。现在有了：`/guide/` 是文章列表，每篇文章在 `/guide/<短名>/`，
加新文章只要往 `src/articles/` 丢一个 markdown 文件。

---

## 交付内容

### 1. 指南栏目改造

| 路径 | 原来 | 现在 |
|---|---|---|
| `/guide/` | 单页 6 条 FAQ | 文章列表（「深入阅读」）+「常见问题速查」 |
| `/guide/<slug>/` | 不存在 | 文章页（新增） |

原来那 6 条 FAQ **一条没删**，FAQPage 结构化数据也保留。

### 2. 三篇文章

| 路径 | 主题 | 字数 | 内链 |
|---|---|---|---|
| `/guide/25t-price/` | 25 吨二手吊车价格区间与影响因素 | 941 | 4 个 |
| `/guide/transfer-process/` | 过户流程（车辆 + 特种设备两条线） | ~1.2k | 3 个 |
| `/guide/hour-meter/` | 工时表五个交叉验证点 | ~1.3k | 3 个 |

内链全部指向站内：`/price/`、`/trucks/25吨/`、`/guide/`、`/sell/`。

### 3. 一套可以持续加文章的工具

- `src/articles/*.md` —— 内容放这里，带 5 字段 frontmatter
- `scripts/lib/md.mjs` —— 零依赖 markdown 渲染器（**没给站加任何依赖**）
- `scripts/build.mjs` —— 构建时自动扫目录、生成页面、更新 sitemap

**加一篇文章的完整操作**：

```bash
# 1. 在 src/articles/ 新建 xxx.md（照抄现有文章的 frontmatter）
# 2. 构建
node scripts/build.mjs
# 3. 验证
node --no-warnings scripts/test-md.mjs
SITE=https://xn--bqr649k.cn node --no-warnings scripts/cdp-articles.mjs
# 4. 推送
node scripts/gh-push.mjs <PAT> mfujun2025/diaoche main
```

### 4. 每篇文章的 SEO 配置

- `title` / `description` / `canonical` 齐全
- **Article** 结构化数据（含 `datePublished`、`publisher`）
- **BreadcrumbList** 面包屑结构化数据
- 已进 `pages-sitemap.xml`（priority 0.6 / changefreq monthly）
- 底部「继续看」互链，站内权重互相流转
- 标题层级严格 h1 → h2 → h3，**正文里不会出现第二个 h1**

---

## 验证结果

| 层 | 脚本 | 项数 | 结果 |
|---|---|---|---|
| 渲染器单测 | `test-md.mjs` | 57 | ✅ |
| 页面无头验证 | `cdp-articles.mjs` | 96 | ✅ 本地、线上各一遍 |
| 原有回归 | test-head / test-auth / test-auth-db | 172 + 200 + 64 | ✅ |
| 部署后体检 | `check-env.mjs` | 8 | ✅ |
| CI | GitHub Actions | 11 步 | ✅ |

无头验证覆盖了：**390×844 与 1280×900 双视口零横向溢出**、
标题层级 h1→h2→h3、**所有内链目标真实可达**（线上发真实请求）、JSON-LD 存在、
面包屑、加粗/列表/引用渲染正确。

---

## 内容红线（三篇都遵守了）

- **价格一律「区间参考」**，与 `/price/` 同一口径，不编造具体成交价
- 每篇只打一个主词，不堆关键词
- 内容具体到能验证（如「查大臂焊接修复痕迹、转台与车架连接处漆面色差」）
- footer 免责声明保留

---

## 你需要做什么

**不用做任何事，已经上线了。**

想自己加文章：
1. `src/articles/` 新建 `.md`（照抄现有文章的 frontmatter 格式）
2. `node scripts/build.mjs`
3. 推送

或者直接跟我说「给吊车.cn 加一篇文章，写 XX 主题」。

---

## 内容节奏建议

一周 2~3 篇，持续 3 个月约 30 篇。
这类站靠**累积长尾词**慢慢起量，通常 2~3 个月才看到稳定自然流量 ——
**别指望发 10 篇就见效**。

下一批选题方向：

- 品牌对比（徐工 vs 三一，二手保值差异）
- 地区市场（江苏 / 山东二手吊车市场在哪）
- 选型（25 吨和 35 吨怎么选）

---

## 顺手修的两处

- `renderSitemap()` 里有一段**从未被使用的死代码**（算了一堆 URL 却没拼进返回值），清掉了；
  两个 sitemap 函数抽成共用一个 `sitemapUrlEntries()`
- 部署后体检新增第 5 项：**文章页没被 SPA 回落吞掉**。
  这条判据不能只看状态码 —— Pages 对不存在的文件也返回 `200 + 首页 HTML`，
  必须核对「页面里有没有这篇文章的独有文字」
