# 下一步操作（老孟照做，约 5 分钟）

> 代码那边我已经全部改完、推上去、CI 跑绿了。**现在只剩 Cloudflare 控制台上 3 步，我这边没有权限，只能你点。**

---

## 背景一句话

后台 404 的锅不在代码，在 Cloudflare：**Pages 自定义域名 + Cloudflare Access 是官方承认的冲突**（Access 的认证回调路径 `/cdn-cgi/access/authorized` 会被 Pages 抢走，必 404）。

所以后台防护方案已经从 Access 换成**应用层密钥**。代码已经上线，现在要把 Access 拆掉、把密钥配上。

---

## 第 1 步：加密钥（必须最先做）

进入控制台：

```
Workers & Pages → 点 diaoche-cn → Settings → Variables and Secrets
```

点 **+ Add**，按这个填：

| 字段        | 填什么                                  |
| --------- | ------------------------------------ |
| **Type**  | 选 **Secret**（⚠️ 不是 Text，Text 是明文可见的） |
| **Name**  | `DC_ADMIN_KEY`                       |
| **Value** | 见下方「你的密钥」                            |

**你的密钥**（直接复制这一整行，别加空格、别换行）：

```
eyOlJRg2TfjP6CYTqLCNIWW8MNhNlmJh
```

- 32 位随机串，本地已用同一份值实测通过（无密钥 401 / 错密钥 401 / 对密钥 200）
- 这台机器写入文件时偶尔会被系统改写，**如果控制台填完登录不进去**，去项目目录下 `.dev.vars` 文件里看实际值，以那个为准。或者你干脆自己生成一个也行：


  ```
  node -e "console.log(require('crypto').randomBytes(24).toString('base64url'))"
  ```
  生成完填进控制台，然后**把它告诉我 / 或自己记好**，登录时用。

填完点 **Save**。

---

## 第 2 步：删掉 Access 应用

> ⚠️ **这是破坏性操作，说明清楚再动手：**
>
> - **删什么**：Zero Trust 里那个保护 `吊车.cn` 的 Access 应用（应用 id `ac961fd9-c24d-4d93-ab26-071a2c4346b6`）
> - **会怎样**：Access 的邮箱验证码登录彻底失效。**这就是我们要的** —— 它本来就走不通
> - **不会怎样**：前台页面、车源数据、D1 数据库、R2 图片**全都不受影响**，只动这一个鉴权入口
> - **能恢复吗**：能，重新建一个 Access 应用即可（但按上面的结论，建了也还是走不通）

路径：

```
Zero Trust → Access → Applications → 找到那个 Destination 含 diaoche-cn.pages.dev 的应用
→ 右侧 ... → Delete → 确认
```

如果它下面还挂着别的应用（比如单独保护 `.pages.dev` 的），**一并删掉**，只要 Destination 里有 `吊车.cn` 或 `diaoche-cn.pages.dev` 的都删。

---

## 第 3 步：重新部署

Secret 是要**部署时才注入**的，加完不重新部署不生效。

```
Workers & Pages → diaoche-cn → Deployments
→ 找到最新那条（应该带 commit 88cfa97）
→ 右侧 ... → Retry deployment
```

等 1~2 分钟跑完。

---

## 做完之后

**跟我说一句，我立刻验证。** 我会跑这套检查：

| 检查项                                   | 期望                       |
| ------------------------------------- | ------------------------ |
| 未登录访问 `/admin/`                       | **200**（不再 302 跳 Access） |
| 未带密钥调 `/api/admin/list`               | **401**                  |
| 带错密钥                                  | **401** + 「访问密钥不正确」      |
| 带对密钥                                  | **200** + 车源列表           |
| 浏览器打开 `https://xn--bqr649k.cn/admin/` | 看到**登录框**，输密钥进后台         |

---

## ✅ 两个地址的坑（都已解决）

1. **推荐用 punycode 地址**：`https://xn--bqr649k.cn/admin/`
   - 输 `吊车.cn/admin` 也行，浏览器会补成 `www.` —— 这个以前会 **522**，**现在已修好**（`www` 已能正常访问）
2. **`www` 前缀已修好**（2026-09-21 验证）：`https://www.xn--bqr649k.cn/` 返回 200
   - 用 `www` 还是不带 `www` 都能进站，两者内容一致
   - 顺手修了一个 bug：以前从 `www` 进入详情页点「查看联系方式」会被拒绝（白名单漏配），现已改为后缀匹配

---

## 顺带：还有几笔账待清（不急，但别忘了）

| 事项                                           | 为什么                                  |
| -------------------------------------------- | ------------------------------------ |
| 吊销 GitHub PAT `ghp_...SATU8`                 | 之前排查时暴露过，虽然仓库是公开的但令牌不该留              |
| 删除 CF token `cfut_Urh2...` / `cfut_zRdge...` | 临时排查用，`cfut_zRdge` 已失效，建议都清掉         |
| 删线上 2 条测试车源（ID #2、#3）                        | 进了后台首页就能看到，点「删除」                     |
| `/api/upload` 与 `/img` 无鉴权                   | 任何人都能传图。不改也不会出事，但以后要防的话可以上 Turnstile |
