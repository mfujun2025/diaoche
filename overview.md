# 车源详情页 —— 已上线

## 你反馈的问题

「点车源看不到详细内容」

**排查结果**：这个站**根本没有详情页**。车源卡片渲染出来就是一整块纯展示，没有链接、没有点击处理；手输 `/trucks/4/` 也不会 404，而是回落到车源大厅页面（看起来像"点了没反应"）。

现在补上了，**已上线**。

---

## 现在能用了

**入口**：车源大厅 → 点任意一张车源卡片 → 进详情页

| 页面 | 地址 |
|---|---|
| 车源列表 | https://xn--bqr649k.cn/trucks/ |
| 详情页（例） | https://xn--bqr649k.cn/trucks/2/ |

**详情页包含**：
- 大图 + 缩略图切换（多图时可左右翻）
- 完整参数表：吨位、品牌型号、出厂年份、工作小时、所在地区、价格、事故记录
- 车况描述
- **查看联系方式**（点一下才显示，默认藏着）
- 「← 返回车源大厅」

**卡片也改了**：整块可点击，hover 有边框高亮，右下角有「查看详情 →」。

---

## 联系方式：按你的要求做了登录门禁（占位）

点「查看联系方式」→ 弹出「登录后可查看联系方式 / 功能开发中」。

**你说的是先做占位、真登录后期再接**，所以我把判断集中在一个函数里，**后期接短信/微信登录只改两处**：

| 文件 | 改什么 |
|---|---|
| `functions/api/truck/[id]/contact.ts` | `requireLogin()` —— 把返回「需要登录」换成真实校验 |
| `public/app.js` | `bindLoginGate()` —— 把占位按钮换成真实登录框 |

前端只认 `401 + needLogin` 这个约定，**不用重构**。

**另外埋了三道拦截**（真实登录接上后依然有效）：
1. 来源校验：Referer 不在白名单直接 403（挡掉最粗暴的直连爬取）
2. 登录门禁
3. 车源必须是已审核状态

---

## 技术决策说明（一个绕过的坑）

**详情页不是构建时预生成的，是访问时实时渲染的。**

原因：这个站是纯静态构建（GitHub Actions 里跑），**构建阶段拿不到数据库数据**，而且车源是动态增删的——每上一条新车源就重新构建一次不现实。

**做法**：用一个 Function 接管 `/trucks/<id>/`，实时查库后把标题、描述、结构化数据（JSON-LD）直接写进 HTML 返回给浏览器 —— 这样**搜索引擎能收录每个车源**，新上架的车源也**立即可访问**，不用等构建。

⚠️ 这里有个坑：接管 `/trucks/*` 会连车源大厅 `/trucks/` 一起接管，所以代码里做了判断「只有纯数字 ID 才渲染详情页，其余回落到原静态页」。**已实测确认 9 个静态页全部正常，没有回归。**

---

## 验证结果

**本地实测**
```
静态页 9 项全部 200        ← 路由改动无破坏
详情页 /trucks/2|3|4/ = 200，/trucks/99999/ = 404
详情接口 200 / 400 / 404 正确
★ 确认 contact 字段未泄漏
联系方式 403（无 Referer）/ 401（合法 Referer）
后台接口回归 200 / 401 正常
UI 7 项通过：卡片可点 → 跳转 → 详情渲染 → 登录占位 → 移动端 390 零溢出
```

**线上实测**
```
全部静态页 200
/trucks/2/ 200，标题 = "25吨 徐工 QY25K5 转让 — 吊车.cn"，含 JSON-LD
/trucks/99999/ 404
/api/truck/2/contact → 401（登录门禁生效）
★ contact 确认未返回
```

CI run **#26 success**，提交 `953df35`。

---

## 补丁（09-21 09:30）：www 域名下拿不到联系方式 —— 已修

### 发现的问题

你的 `www` 前缀已经配好了（`www.xn--bqr649k.cn` 现在返回 200），但**从前台点「查看联系方式」会一直失败**。

原因：`contact` 接口的来源校验白名单是**写死的**，只认 `xn--bqr649k.cn` 和 `diaoche-cn.pages.dev`。
你从 `www.` 进来，浏览器带过去的 `Referer` 是 `www.xn--bqr649k.cn`，**不在名单里 → 直接 403**。

另一个隐患：`www` 站点上的「预发子域」（`abc123.diaoche-cn.pages.dev` 这类每次部署都变的地址）也不在名单里。

### 修法

白名单从「逐个写死域名」改成**后缀匹配**：

```
host === 'xn--bqr649k.cn' || host.endsWith('.xn--bqr649k.cn')
```

这样 `www` / `m` / 以后任何子域都自动覆盖，**不会再出现「加了域名但白名单漏配」这种问题**。

### 验证（本地 + 线上各 8 项）

| Referer | 结果 |
|---|---|
| 无 Referer | 403 ✅ |
| `www.xn--bqr649k.cn` | 401（登录门禁）✅ **本次修复的** |
| `xn--bqr649k.cn` | 401 ✅ |
| `diaoche-cn.pages.dev` | 401 ✅ |
| `abc123.diaoche-cn.pages.dev` | 401 ✅ |
| `evil.com` | 403 ✅ |
| `xn--bqr649k.cn.evil.com` | 403 ✅ 后缀匹配没被绕过 |
| `evilxn--bqr649k.cn` | 403 ✅ 前缀拼接也没被绕过 |

全站 16 个路径 + www 域 3 个路径全部 200，`/trucks/99999/` 仍 404，contact 字段确认未泄漏。

CI run **#28 success**，提交 `9fb0739`。

---

## 待办

| 事项 | 说明 |
|---|---|
| **接真实登录** | 需先定：短信服务（要花钱+实名）还是微信开放平台（要企业主体） |
| 删重复的 `DC_ADMIN_KEY` Secret | 控制台里加了两条同名，删掉一条 |
| ~~修 `www` 前缀 522~~ | ✅ 已完成（09-21 已验证 200） |
| 清理测试车源（#2 #3 #4） | 进后台点删除 |
| 吊销 GitHub PAT | `ghp_...SATU8` |
