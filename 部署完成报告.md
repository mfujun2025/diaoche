# 吊车.cn 部署完成报告

## 结论

**站点已成功上线。** 线上地址：**https://diaoche-cn.pages.dev/**

CI 流水线全绿（10/10 步骤），D1 数据库已建表并绑定，站点功能已线上实测通过。

---

## 一、我代做的部分（全部完成）

| 项 | 结果 |
|---|---|
| 校验 CF 凭据 | ✅ token 有效 |
| 修正 Account ID | ✅ 从 API 读取真实值（截图里那个是错的） |
| 创建 D1 数据库 | ✅ `diaoche-db` |
| 线上建表 | ✅ 2 张表 + 3 个索引 |
| 回填 `wrangler.toml` | ✅ database_id 已写入并推送 |
| 创建 Pages 项目 | ✅ `diaoche-cn` |
| 配置 D1 绑定 | ✅ production + preview 双环境 |
| 配置环境变量 | ✅ `ENV` / `SITE_NAME` / `ADMIN_EMAILS` |
| 写入 GitHub Secrets | ✅ 两条均已加密写入 |
| 触发并验证 CI | ✅ 全绿 |
| 线上功能实测 | ✅ 10 项通过 |
| 清理测试数据 | ✅ trucks count = 0 |

---

## 二、中间遇到的一个阻塞（已解决）

CI 推到最后一刻失败了：

```
✘ [ERROR] Failed to publish your Function.
  R2 bucket 'diaoche-images' not found.
```

**根因**：你的 CF API Token **没有 R2 权限**。我实测确认过——wrangler CLI 和直连 API 都报 `10000 Authentication error`，是权限问题不是工具问题。

**怎么处理的**：你选了「先去掉 R2 绑定把站上线」。我之前核查过 `functions/` 目录里**零处引用** `env.IMAGES`，前端图片上传也还没接，所以去掉零影响。R2 那段在 `wrangler.toml` 里**注释保留**了，将来补上权限取消注释即可。

---

## 三、线上实测结果（10 项全通过）

| 测试项 | 结果 |
|---|---|
| 首页 `GET /` | HTTP 200 |
| 页面标题 | `吊车.cn — 全国二手吊车转让信息平台` |
| `/trucks/` `/sell/` `/rent/` `/price/` `/guide/` | 全 HTTP 200 |
| 车源列表 `GET /api/trucks` | `{"ok":true,"total":0,"data":[]}` |
| 带筛选 `?biz_type=sale&tonnage=25` | 正常返回 |
| 提交 `POST /api/submit` | `{"ok":true,"msg":"提交成功，审核通过后展示"}` |
| 校验：吨位 9999 | 拒绝，`吨位需为 1~2000 的数字` |
| 校验：缺联系方式 | 拒绝，`联系方式必填且不超过 64 字` |
| **审核隔离** | 提交后数据库里是 `pending`，但前台列表仍 `total: 0` |
| 后台接口无凭证 | 401 `未通过 Cloudflare Access 认证` |
| 后台接口伪造凭证 | 401 `身份令牌解析失败` |

**核心结论**：数据链路（提交→落库→审核隔离→前台过滤）和鉴权层都工作正常。

---

## 四、还要你手动做 3 件事

### 1. 绑定自定义域名

页面还不能用 `吊车.cn` 访问，因为还没绑。

**Pages 项目 → Custom domains → Set up a domain**，填：

```
xn--bqr649k.cn
```

⚠️ 必须填 punycode 形式，**不能写中文**。前提是 `吊车.cn` 的 NS 已经指向 Cloudflare（如果还没，要先去域名注册商改 NS）。

### 2. 配 Access 保护后台（**安全关键，优先做**）

⚠️ **现在 `/admin/` 和 `/api/admin/*` 是公开可访问的**，任何人都能进去。原因是 Access 没配，而且 `ADMIN_EMAILS` 是空的——两层防线都没生效。

**Zero Trust → Access → Applications → Add → Self-hosted**，要建**两个**应用：

**应用 A（生产域名）**
- Public hostname：裸域 `xn--bqr649k.cn`
- Path 填 `admin`，**再加一条** Path 填 `api/admin`

**应用 B（预览域名，千万别漏）**
- Subdomain `diaoche-cn`，Domain `pages.dev`
- Path 填 `admin`，**再加一条** Path 填 `api/admin`

> 只保护生产域名等于留后门 —— `.pages.dev` 是公开可访问的，别人从那里一样能进后台。

**策略**：每个应用配一条 `Allow` 策略，Include → Emails → 填你的邮箱。

### 3. 填 `ADMIN_EMAILS`

**Pages 项目 → Settings → Variables and Secrets** → 编辑 `ADMIN_EMAILS`，填你的邮箱（多个用逗号分隔）。

这层是代码里的白名单校验，即使 Access 被绕过也拦得住。**现在是空的，等于没生效。**

---

## 五、后置项：R2 图片存储

等你有空了再弄，不影响现在用。

**恢复步骤**：
1. 编辑 CF API Token，加权限 `Account / Workers R2 Storage / Edit`
2. 控制台 `R2` → Create bucket → 名称 `diaoche-images`
3. **同步更新 GitHub Secret** `CLOUDFLARE_API_TOKEN`（关键，否则 CI 还是用旧 token）
4. 取消 `wrangler.toml` 里 R2 那段的注释，推送

---

## 六、安全提醒

**那个 PAT `ghp_...SATU8` 已经明文出现在对话里，请尽快吊销。**

GitHub → Settings → Developer settings → Personal access tokens → 找到它 → Delete。

以后如果要我推代码，重新生成一个（勾 `repo` + `workflow`）发我即可。

另外，CF API Token 也出现在对话里了，但它是**功能性的**——CI 正在用。如果你在意，可以吊销后重建一个（权限照上表配），重建后记得**同步更新 GitHub Secret**。

---

## 七、仓库最新提交

本轮推送记录（远端 `main` 现在指向 `2a74389`）：

| commit | 内容 |
|---|---|
| `a097c53` | 格式化 SETUP-CHECKLIST 表格 |
| `ec0dee7` | 回填 D1 database_id |
| `ff1b1ea` | 临时移除 R2 绑定，让站点先上线 |
| `2a74389` | 更新文档：部署状态、实测结果、待办清单 |

详细操作清单见仓库里的 `SETUP-CHECKLIST.md`（已重写为「已完成 / 待操作」两段式）。
