# 交付报告：静态资源内容哈希（2026-09-21）

## 你遇到的问题

打开车源详情页，登录那一块还显示旧占位「登录（开发中）」，刷新、关标签页重开都没用。

## 根因不是代码，是缓存

| 资源类型 | CF Pages 给的默认响应头 | 后果 |
|---|---|---|
| HTML 页面 | `max-age=0` | 每次回源 → **拿到的是新版** |
| JS / CSS | `public, max-age=14400` | 浏览器 **4 小时内完全不回源** → **还是旧版** |

于是出现「新版 HTML + 旧版 JS」的组合：页面骨架是新的，登录卡片还是几个月前的占位。
你自己怎么刷新都没用 —— 浏览器根本没去问服务器。

## 试过并且失败的路：改 `_headers`

`public/_headers` 是 CF Pages 官方提供的自定义响应头机制。我写了：

```
/app.js
  Cache-Control: public, max-age=0, must-revalidate
```

为了确认文件到底有没有被处理，我加了两个自定义探针头（`X-DC-Probe-Global`、`X-DC-Probe-Appjs`）。
线上实测结果：

- 两个探针头 **都返回了** → 说明 `_headers` 文件**确实被处理了**
- 但 `Cache-Control` **依然是** `max-age=14400` → **这个字段被资源层锁死，改不动**

官方文档说可以覆盖，在这个项目上不行。**结论：别在 `Cache-Control` 上继续耗。**

## 最终方案：文件名带内容哈希

前端的标准做法。内容一变，文件名就变，浏览器和 CDN 都不可能命中旧副本 ——
不再依赖任何响应头。

```
dist/app.0c06e2fd.js      ← 内容 sha256 前 8 位
dist/admin.fb95e297.js
dist/style.fa29d6c5.css
dist/assets.json          ← { app, admin, css } 的映射清单
```

页面里引用的就是带哈希的名字：

```html
<script src="/app.0c06e2fd.js" defer></script>
```

## 改了哪些文件

| 文件 | 改动 |
|---|---|
| `scripts/build.mjs` | 新增 `fingerprintAssets()`：算哈希、写哈希文件、输出 `assets.json`；页面模板改用 `assets.app` / `assets.admin` |
| `functions/trucks/[[path]].ts` | 原来写死 `/app.js`、`/style.css`，改为读 `dist/assets.json`（打包时内联，运行时零开销） |
| `public/_headers` | 清掉探针，只留「HTML 不缓存」一条，并写下这次的实测结论 |
| `scripts/check-env.mjs` | 第 6 项从「查缓存头」改成「查指纹一致性」 |

### 关于车源详情页的那个坑

车源详情/长尾页是 **Functions 实时渲染**的，它拿不到构建期的变量，
所以我把映射写进 `dist/assets.json`，让 Function 在打包时把它内联进去。
已用 `wrangler pages functions build` 验证产物里确实出现了哈希路径。

⚠️ 由此多一条硬约束：**必须先 `npm run build` 再部署**，否则 `dist/assets.json` 不存在会打包失败。
CI 里 build 在 deploy 之前，没问题。

## 验证结果

- 本地构建通过：`[build] 资源指纹: /app.0c06e2fd.js /admin.fb95e297.js /style.fa29d6c5.css`
- wrangler 打包通过，JSON 已内联进 Functions 产物
- 直传部署成功 → 线上体检 **14 项全绿**（此前两次 CI 报红正是卡在缓存头那一项）
- `/trucks/5/` 已引用 `/app.0c06e2fd.js` + `/style.fa29d6c5.css`，页面里**不再有**「登录（开发中）」，
  新版 app.js 里已有 `/api/auth/send-code`
- commit `34e4f69` 已推送（远端 `2019dcf`），**CI success**

## 你现在该怎么验证

**不用硬性刷新，直接正常打开就行** —— 这就是这次改动的目的：

1. 打开 https://xn--bqr649k.cn/trucks/5/ （或任意车源详情页）
2. 看登录区：应该出现「邮箱 + 获取验证码」的真实表单，不再是「登录（开发中）」
3. 想确认的话：右键 → 检查 → Network，看 JS 文件名是不是带了一串字母数字

如果还是旧样子，那是浏览器把 **HTML** 也缓存了（极少见），硬刷一次即可（Ctrl+Shift+R）。

## 顺带说明：为什么无哈希的旧名字还留着

`/app.js`、`/style.css` 这些稳定名我没有删，作为兜底 ——
万一有人只部署 Functions 没跑构建，页面不至于 404 裸奔。
新页面一律引用哈希名，多出来的两份文件不会影响任何东西。
