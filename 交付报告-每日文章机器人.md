# 交付报告：吊车.cn 每日文章机器人

## 一句话

每天 09:00 自动挑一个选题 → 生成一篇 1000~1500 字的吊车文章 → 校验 → 构建验证 → 提交到仓库，现有 CI 自动构建部署上线。全程零 npm 依赖。

---

## 流水线

```
GitHub Actions cron（每天 UTC 01:00 = 北京 09:00）
   ↓
① 选题  topics.mjs   —— 从 60 个选题池里挑一个没写过的（类别轮转）
   ↓
② 生成  llm.mjs      —— OpenAI 兼容协议，温度/字数/结构/红线都写进 prompt
   ↓
③ 校验  validate.mjs —— 24 类规则，不通过就重生成（最多 3 次 + 退避）
   ↓
④ 构建  build.mjs    —— 真的构建一遍站点，构建不过就撤草稿
   ↓
⑤ 发布  publish.mjs  —— Git Data API 精确提交**这一个文件**（不会带上别的改动）
   ↓
⑥ 日志  logger.mjs   —— logs/article-bot.jsonl + 失败时 webhook 告警
```

推到仓库后，**现有的 Build & Deploy 工作流自动接管**，所以这里不需要再写一套部署。

---

## 三种运行方式

```bash
# 1. 离线自测（不联网、不花 token、不碰仓库任何文章）
LLM_PROVIDER=mock node scripts/bot/run.mjs --dry-run

# 2. 真机演练 —— 会调模型、会校验、会构建，但不推送
LLM_API_KEY=sk-xxx node scripts/bot/run.mjs --dry-run

# 3. 正式发布
LLM_API_KEY=sk-xxx ARTICLE_BOT_GH_TOKEN=ghp_xxx node scripts/bot/run.mjs
```

其他命令：

```bash
node scripts/bot/run.mjs --list-topics                 # 看还剩哪些选题
node scripts/bot/run.mjs --topic=select-25-vs-35       # 指定写哪一篇
node scripts/test-bot.mjs                              # 52 项离线自测
```

---

## 可靠性怎么做的

| 环节 | 做法 |
|---|---|
| **失败重试** | 生成 + 校验整体重试 3 次，退避 3s / 10s / 25s；模型返回非 JSON、JSON 解析失败、网络超时都算一次重试 |
| **发布前校验** | 24 类规则，见下表。**不通过就不发**，不会上线一篇半成品 |
| **构建验证** | 写完 md 立刻跑 `scripts/build.mjs`，构建失败就把草稿删掉并告警 |
| **去重** | 双来源：仓库里的 `src/articles/*.md`（文章本体）+ 日志里的 `topicId`。**不依赖状态文件**（状态文件最容易丢，一丢就重发） |
| **覆盖保护** | 提交前查远端同名文件，存在就拒绝覆盖 —— 日志丢了也不会洗掉老文章 |
| **日志** | `logs/article-bot.jsonl`，每行一条 JSON：时间、选题、标题、slug、字数、成功/失败、失败原因、耗时 |
| **告警** | 生成失败 / 构建失败 / 发布失败 / 选题池耗尽 → 飞书或企业微信机器人 |
| **退出码** | 0 成功 / 1 业务失败 / 2 配置错误，CI 能精确判断 |

### 发布前的 24 类校验（挑几条关键的）

| 校验 | 为什么查这条 |
|---|---|
| 正文出现主关键词 ≤5 次、核心词密度 0.8%~2.8% | 关键词堆砌会被判低质量 |
| 禁止表格 / 图片 / 代码块 / 缩进嵌套列表 | 站内零依赖渲染器不支持，写了会在页面上原样火星文 |
| `title` 不许有英文冒号 | frontmatter 按「第一个英文冒号」切字段，有冒号会被截断 |
| 命中套话黑名单（"综上所述""以上就是"…） | 这类 AI 味开头结尾最掉档次 |
| 不许出现「成交价 28.5 万」「上周成交一台」 | 价格红线：只能写区间参考，不许编造具体成交 |
| 小标题 3~5 个、内链 3~4 个且必须站内 | 站内容结构与内链规范 |
| 渲染后不能残留 `**`/`|`/```` ```` | 直接证明渲染器吃得下这篇稿子 |

---

## ★ 需要你补充确认的配置项

配置全在 `config/article-bot.json`，**密钥一个都不写在里面**，全部是环境变量。

| # | 项 | 环境变量 | 必填 | 说明 / 我的建议 |
|---|---|---|---|---|
| 1 | **大模型接口与密钥** | `LLM_API_KEY`<br>`LLM_BASE_URL`<br>`LLM_MODEL` | ✅ | 用哪家？我默认填的是 DeepSeek（`https://api.deepseek.com/v1`、`deepseek-chat`），中文长文质量够、价格低。**只要 OpenAI 兼容协议的都能用**（通义、智谱、Moonshot、OpenAI、本地 Ollama）。通义的话 base 是 `https://dashscope.aliyuncs.com/compatible-mode/v1` |
| 2 | **GitHub 写入凭据** | `ARTICLE_BOT_GH_TOKEN` | ✅ | ⚠️ 建议用你的 **PAT（repo 权限）**，别用 Actions 内置的 `GITHUB_TOKEN` —— 内置 token 的提交**不会触发**其它工作流，文章会停在仓库里不上线（我已经加了兜底部署步骤保底，但 PAT 更干净） |
| 3 | **执行时间** | — | 可选 | 现在是 `0 1 * * *` = 北京时间 09:00。改的话要**同时**改 `config/article-bot.json` 的 `schedule.cron` 和 workflow 文件里的 cron |
| 4 | **告警机器人** | `ARTICLE_BOT_WEBHOOK` | 强烈建议 | 飞书/企业微信群里加个机器人，把 webhook 地址填进 Secrets。跑挂了能第一时间知道，否则只能靠翻 Actions 页面 |
| 5 | **要不要人工审核再发** | `publish.target` | 建议定 | 现在是全自动发布。如果你担心质量，改成 `"dir"` —— 文章写进 `out/articles/`，你过目后再决定是否发 |
| 6 | **选题池扩充** | — | 2 个月后 | 现在 60 个选题（选型/安全/维保/故障/案例/趋势 各 10 个），每天一篇能跑**两个月**。用尽会告警停跑，到时候给我一句话，我按你实际在推的吨位/地区扩充 |
| 7 | **是否改成接口发布** | `ARTICLE_API_URL`<br>`ARTICLE_API_TOKEN` | 可选 | 如果以后想把文章直接写进 D1 或外部 CMS，把 `publish.target` 改成 `http` 并填这两个变量即可，代码不用动 |

###  Secrets 怎么填

GitHub 仓库 → Settings → Secrets and variables → Actions → New repository secret：

```
LLM_API_KEY              = sk-xxxxxxxx
LLM_BASE_URL             = https://api.deepseek.com/v1      （可选，不填用配置里的默认值）
LLM_MODEL                = deepseek-chat                    （可选）
ARTICLE_BOT_GH_TOKEN     = ghp_xxxxxxxx                     （建议 PAT）
ARTICLE_BOT_WEBHOOK      = https://open.feishu.cn/open-apis/bot/v2/hook/xxx   （可选）
```

---

## 建议：先这样跑一周

直接全自动每天上线一篇，风险在于模型偶尔会写出你不满意的东西。更稳的做法：

1. 第一周把 Secrets 填好，用 Actions 页面的 **Run workflow** 手动触发，勾 `dry_run`
2. 每天看一眼 output 里那篇 1000 多字的内容，觉得 OK 再取消勾选正式发布
3. 摸清质量稳定后，放开每天 09:00 自动跑

---

## 已知限制（不是 bug）

| 限制 | 影响 | 怎么应对 |
|---|---|---|
| **GitHub cron 会延迟** | 声明 09:00，实际可能 09:05~09:30，高峰期偶尔更晚 | 对内容站无所谓。要精确就得用外部 cron（如 CF Worker Cron）或服务器计划任务 |
| **仓库长期无活动会停用计划任务** | 连续 60 天没提交，GitHub 会停用 scheduled workflow | 项目一直在更新，触发不了；真停了 GitHub 会发邮件 |
| **模型质量波动** | 偶尔写得水 | 校验只守底线不保证上限；人工抽检一周最稳 |
| **选题池会用尽** | 两个月后停跑 | 告警会提醒；给句话我就补 |
| **文章上线后不自动重提搜索引擎** | 新文章被 crawl 需要时间 | 配好 Bing 的 IndexNow 后可以顺手加一步推送（现在还没配） |

---

## 代码结构

```
config/article-bot.json            全部配置（时间/目标/选题/字数/阈值）
config/article-bot.example.json    配置样例，照着改
scripts/bot/run.mjs                编排入口 + CLI
scripts/bot/lib/config.mjs         配置加载 + 运行时前置检查
scripts/bot/lib/llm.mjs            OpenAI 兼容调用 + 离线 mock
scripts/bot/lib/topics.mjs         选题轮转 + 相似度去重
scripts/bot/lib/validate.mjs       24 类发布前校验
scripts/bot/lib/publish.mjs        git / http / dir 三种发布目标
scripts/bot/lib/logger.mjs         JSONL 日志 + webhook 告警
scripts/bot/lib/net.mjs            代理与 TLS（本机调试用）
scripts/test-bot.mjs               52 项离线自测（已接入主 CI）
.github/workflows/daily-article.yml  每天 UTC 01:00
```

自测 52 项全绿，主 CI（Linux）也已通过。
