# 交付报告：「吨位 × 地区」长尾路径页

**日期**：2026-09-21
**提交**：`2a6842b`（GitHub Actions 构建成功，已部署 Cloudflare Pages）
**验证**：线上实测 + 本地逻辑单测 + CDP 无头 DOM 断言，全部通过

---

## 一、做了什么

把原来的**参数筛选页**（`/trucks/?tonnage=25&province=江苏`）升级为**静态路径页**（`/trucks/25吨/江苏/`），并铺开长尾入口网络。

### 为什么必须做

原来的参数页有三个硬伤：
1. `robots.txt` 里 `Disallow: /*?*` 把参数页全部屏蔽了 —— 一条都收不进去
2. 同一批车源对应无数 URL（参数顺序不同、有无默认值不同），权重分散
3. 没有独立 title / description / canonical，搜索引擎不认它是独立页面

改路径页后：**每个组合一条独立 URL、独立标题、独立结构化数据**，可以吃「25吨吊车 江苏」「徐工 25吨二手」这类精准长尾词。

---

## 二、支持的 URL 形态

| 形态 | 示例 | 说明 |
|---|---|---|
| 吨位 | `/trucks/25吨/` | 兼容 `25` / `25t` 写法 |
| 地区 | `/trucks/江苏/` | 兼容 `江苏省` / `上海市` 写法 |
| 品牌 | `/trucks/徐工/` | |
| 吨位+地区 | `/trucks/25吨/江苏/` | **主战场**，长尾词最精准 |
| 吨位+地区+品牌 | `/trucks/25吨/江苏/徐工/` | 有车才出 |

**乱序访问会自动归一**：`/trucks/江苏/25吨/` 的 canonical 指向 `/trucks/25吨/江苏/`，不会产生重复内容。

---

## 三、四条核心设计决策

### 1. 空组合一律 404，不进索引

9 个吨位 × 8 个地区 × 4 个品牌 = 288 个组合，绝大多数没车。若全部无脑生成，搜索引擎判为 thin content（低质量页），**反而拖累整站评分**。

所以：查库结果为 0 → 返回 404 + `noindex,nofollow`，且**绝不写进 sitemap**。

### 2. canonical 固定按「吨位/地区/品牌」顺序拼

避免 `/trucks/江苏/25吨/` 和 `/trucks/25吨/江苏/` 被当成两个页面分散权重。

### 3. 结构化数据用 `CollectionPage` + `ItemList`

详情页用 `Product`，列表页用 `CollectionPage`。前者告诉搜索引擎「这是一个商品」，后者告诉它「这是一个聚合列表，里面第 1 项是什么」—— 语义不能混。

### 4. 内链织成网（爬虫可遍历）

- 首页：吨位 / 地区 / 品牌三组入口 + 搜索框改为直接跳路径页
- 车源大厅：同样三组入口
- 每个长尾页：`buildRelated()` 输出「同层级换个维度」的 8-12 个链接（如 25吨江苏页 → 链接到 8吨江苏、12吨江苏…）
- 每个详情页：向上链接到它所属的吨位页、地区页、组合页

---

## 四、改动的文件

| 文件 | 改动 |
|---|---|
| `functions/trucks/[[path]].ts` | **重写**。详情页 + 长尾页合并分流，新增 `parseSeg` / `parsePath` / `canonPath` / `buildRelated` |
| `functions/trucks-sitemap.xml/index.ts` | 新增长尾组合页聚合查询（`UNION ALL` 一次查完三种粒度） |
| `src/site.mjs` | 首页 / 车源大厅加三组长尾内链；搜索框目标改为路径页 |
| `public/app.js` | 新增 `parseDataQuery()`（读服务端写进 `data-query` 的筛选条件）；`goSearch()` 改跳路径页 |
| `src/style.css` | 新增 `.crumb` 面包屑样式（原先只在详情页行内） |
| `scripts/test-longtail.mjs` | **新增**。19 条路径解析断言 + canonical 归一测试 |
| `scripts/cdp-check.mjs` | **新增**。CDP 无头读 DOM 断言，验证 4 个页面真实渲染 |
| `scripts/gh-runs.mjs` | **新增**。查 Actions 状态（本机 `gh` 不可用，走 Node + 代理） |

---

## 五、线上实测结果

### 状态码（用车源数据：25吨徐工江苏 ×2、25吨徐工上海 ×1）

| URL | 状态 | 判定 |
|---|---|---|
| `/trucks/25吨/江苏/` | 200 | ✅ 有车 |
| `/trucks/25吨/` | 200 | ✅ 有车 |
| `/trucks/江苏/` | 200 | ✅ 有车 |
| `/trucks/徐工/` | 200 | ✅ 有车 |
| `/trucks/35吨/` | **404** | ✅ 无车，正确排除 |
| `/trucks/25吨/山东/` | **404** | ✅ 组合不匹配 |
| `/trucks/2/` | 200 | ✅ 详情页未受影响 |
| `/trucks/` | 200 | ✅ 大厅未受影响 |
| `/trucks-sitemap.xml` | 200 XML | ✅ |

### SEO 信息（`/trucks/25吨/江苏/`）

```
title:     25吨 江苏 二手吊车转让车源（共2条） — 吊车.cn
canonical: https://xn--bqr649k.cn/trucks/25吨/江苏/
og:url:    同上
JSON-LD:   CollectionPage, numberOfItems=2
           ListItem[1] → /trucks/3/  「25吨 徐工 QY25K5」
           ListItem[2] → /trucks/2/  「25吨 徐工 QY25K5」
```

### CDP 无头 DOM 断言

```
长尾页 /trucks/25吨/江苏/   卡片 2 张 · 内链 8 个   ✓
长尾页 /trucks/25吨/        卡片 3 张 · 内链 8 个   ✓
车源大厅 /trucks/           卡片 3 张 · 内链 21 个  ✓
详情页 /trucks/2/           正文+免责声明 · 内链 4 个 ✓
```

---

## 六、踩到的坑（已写进技能）

### 1. 本地 `wrangler pages dev` 完全不跑 Functions

**症状**：`/api/trucks` 返回 JSON 正常，但 `/trucks/2/`、`/trucks/25吨/` 全部返回 `text/html`（首页）。

**原因**：`dist/_routes.json` 由 `public/` 目录拷进 `dist`，但 wrangler 启动时读取的路由表与之一致的情况下仍然把 `/trucks/*` 交给了静态资源。加 `exclude: ["/trucks/"]` 也无效。

**结论**：**本机 wrangler dev 不可信**。线上环境（真实 Cloudflare Pages）是正常工作的 —— 同一个 commit，线上 `/trucks/2/` 直出了正确的 SEO 标题。

**教训**：验证 Functions 行为，要么直接部署到线上实测，要么用纯逻辑单测把解析函数摘出来跑。不要在本机 wrangler dev 上花时间调试。

### 2. curl 走本机代理全部失败

**症状**：`curl --proxy http://127.0.0.1:27497` 返回 `http=000`，exit code 35（SSL connect error）。

**绕过**：用 Node 的 `undici.ProxyAgent` + `fetch`（与 `gh-push.mjs` 同一条通道），HTTP 200 正常。

### 3. 对象键顺序干扰测试断言

`parsePath(['江苏','25吨'])` 返回 `{province, tonnage}`，而 `parsePath(['25吨','江苏'])` 返回 `{tonnage, province}` —— 用 `JSON.stringify` 直接比对会误报失败。改用排序后逐键比较。

---

## 七、老孟侧待办（与本任务无关，但顺手提醒）

线上 D1 现有 3 条车源，**id=2 和 id=3 是完全重复的测试数据**（都是 25吨徐工 QY25K5 / 2019 / 江苏南京 / 38万）。另外 id=4 的车况描述内容是乱码测试文本。

这些建议删掉 —— 删除后长尾页会自动收缩（`/trucks/25吨/` 从 3 条变 1 条），sitemap 也会自动同步，**不需要重新构建**。
