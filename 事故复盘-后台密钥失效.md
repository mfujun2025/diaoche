# 事故复盘：后台突然进不去（DC_ADMIN_KEY 失效）

**时间**：2026-09-21
**现象**：访问 `/admin/` 显示「后台未配置访问密钥，请先设置 DC_ADMIN_KEY」，密码框输了也没用
**状态**：✅ 已修复并加固（新增部署后自动体检，同类问题以后会 CI 红灯）

---

## 一句话结论

**后台密钥 `DC_ADMIN_KEY` 在线上变成了空值**，所以 `/api/admin/*` 全部 401。
不是代码问题，也不是你输错了密码 —— 是**服务器读到的密钥是空的**。

---

## 排查过程

### 第一步：确认「不是代码问题」

线上直接打接口，带正确的密钥：

```
POST /api/admin/login  → 401 {"msg":"后台未配置访问密钥，请先设置 DC_ADMIN_KEY"}
GET  /api/admin/list   → 401 {"msg":"后台未配置访问密钥，请先设置 DC_ADMIN_KEY"}
```

关键点：**三个不同接口返回的是同一句话**，而且这句话来自代码里的
fail-closed 分支 —— 说明代码读到的是**空字符串**，压根没走到密钥比对。

代码逻辑（`functions/api/admin/_auth.ts`）：

```ts
const expected = (env.DC_ADMIN_KEY || '').trim();
if (!expected) return { ok: false, reason: '后台未配置访问密钥，请先设置 DC_ADMIN_KEY' };
```

→ 判定：**环境变量在运行时是空的**。

### 第二步：确认「变量确实在，但值是空的」

用 CF API 读项目配置：

```
DC_ADMIN_KEY      [secret_text]  ← 变量名在
DC_MAIL_DEV_MODE  [secret_text]  ← 变量名在
ENV               [plain_text] value="production"
SITE_NAME         [plain_text] value="吊车.cn"
```

⚠️ 这里我**一度误判**：`secret_text` 类型读回来的 `value` 永远是 `""`，
这是 CF 的正常设计（不回显密钥明文）。所以「看到空值」本身不能证明变量丢了。

**真正的判据只能是打接口实测**：
- 返回「未配置访问密钥」= 变量真的是空的
- 返回「缺少访问密钥」= 变量正常（只是没带密钥）

用这个标准复核，确认**变量值真的是空的**。

### 第三步：定位为什么修了不生效

我先把正确的密钥写回项目配置（API 返回 `success: true`），
但接口**依然 401**。然后尝试用 API 让部署重跑：

```
POST /deployments/:id/retry
→ 400 {"code":8000055,
       "message":"You cannot retry a Direct Upload deployment.
                  Retries are only possible for builds."}
```

**这条报错解释了全部现象**：

这个 Pages 项目用的是 `wrangler pages deploy`（**Direct Upload / 直传部署**），
查项目配置 `source: undefined` —— **它根本没连 Git 仓库**。

于是有了三个连锁后果：
1. 直传部署**无法 retry**（所以你在控制台点 Retry 也没用）
2. 直传部署**不携带项目级环境变量**
3. 改完变量**必须重新触发一次部署**才会生效

### 第四步：修复

1. 用 CF API 把 `DC_ADMIN_KEY` 写回正确值
2. 触发一次 CI 部署
3. 验证：

```
GET /api/admin/list  → 200
{"ok":true,"counts":{"pending":0,"approved":4,"rejected":0}, "data":[...]}
```

✅ **后台恢复正常，数据都在**（已通过 4 条、待审 0 条）。

---

## 根因：变量是什么时候变空的？

**无法 100% 确认**（CF 不提供变量变更历史），但最可能是：

> 09-20 你在控制台加 `DC_MAIL_DEV_MODE` 时，**变量编辑页是整表提交的**，
> 手滑把 `DC_ADMIN_KEY` 的值一起清空了。

同一个页面上，`DC_MAIL_DEV_MODE` 现在也是空值 —— 和你后来「删掉它」的
操作对得上，说明那个页面确实被整体编辑过。

**要点：控制台改变量要小心它会把整张表一起提交。**

---

## 这次暴露的真问题：CI 看不见配置故障

比变量丢更值得警惕的是这个：

> **CI 从构建到部署全程绿灯，却完全不知道后台已经进不去了。**

CI 只验证「代码编译过、文件传上去了」，它**不知道运行时的环境变量对不对**。
这类配置故障不在 CI 的视野里 —— 最后是你自己点开后台才发现的。

所以这次不只是修变量，还加了**部署后自动体检**。

---

## 加固：部署后自动体检

新增 `scripts/check-env.mjs`，CI 里部署完自动跑，检查四项：

| # | 检查项 | 判据 | 致命 |
|---|---|---|---|
| 1 | `DC_ADMIN_KEY` 是否生效 | 返回「未配置访问密钥」= 丢了 | ★ 是 |
| 2 | 发码接口可用 + `DC_MAIL_DEV_MODE` 未误开 | 500 = 表缺失；响应带 `devCode` = 安全洞 | ★★ 是 |
| 3 | `/api/auth/me` 正常 | 匿名应返回 `loggedIn:false` | 否 |
| 4 | 详情页门禁契约 | 必须 `401 + needLogin` | 是 |

**没通过 → CI 直接报红。** 以后同类问题会在部署时就暴露，不用等你发现。

### 已做反向验证

光加脚本不够，得证明它真能抓到问题。验证方式：改坏变量 → 跑体检 →
确认报红 → 恢复。判据本身也核对过：

```
变量正常时：401 {"msg":"缺少访问密钥"}          ← 体检判为 ✅
变量丢失时：401 {"msg":"后台未配置访问密钥…"}   ← 体检判为 ❌
```

两者区分得开，脚本不是装饰品。

---

## 你需要做的

### ① 去控制台删两个变量（API 删不掉）

Cloudflare 控制台 → Workers & Pages → diaoche-cn → Settings → Variables and Secrets：

| 变量 | 环境 | 处理 |
|---|---|---|
| `DC_MAIL_DEV_MODE` | Production | **删除**（空值残留，且它本身是调试开关，留着有误开风险） |
| `ADMIN_EMAILS` | Preview | **删除**（Access 时代的废弃变量，代码已不读取） |

⚠️ 编辑时**看清楚别碰到 `DC_ADMIN_KEY`** —— 变量表是整体提交的。

### ② 彻底根治：让项目连上 Git 仓库（可选但推荐）

现在的直传部署有三个先天缺陷：无法 retry、不带变量、改配置必须重部署。

正解是在网页端把项目改成**连接 GitHub 仓库**：
Cloudflare 控制台 → Workers & Pages → diaoche-cn → Settings → Build → 
Connect to Git → 授权 CF 的 GitHub App → 选 `mfujun2025/diaoche`

配好后：
- 构建命令 `node scripts/build.mjs`
- 输出目录 `dist`
- 之后 push 由 CF 自己构建，环境变量由平台管理，**再也不会被覆盖**

> 这一步**必须你在网页端做** —— CF 的 Git 集成依赖 OAuth 授权，
> 纯 API 建不了（我实测过，`/source` 端点返回 `method_not_allowed`）。
> 做完告诉我，我帮你验证构建配置对不对。

⚠️ 注意：CF 免费版**每月 500 次构建**，对本站够用。

---

## 顺带修好的

- `scripts/check-env.mjs` —— 部署后体检（新增）
- `.github/workflows/deploy.yml` —— 部署后自动跑体检（新增两步）
- 技能 `cf-pages-full-deploy` —— 新增 §4.1/4.2 环境变量实测判据、
  §14.10/14.11 部署后体检与反向验证，修正了原先「PATCH 是覆盖式」的错误说法
  （实测是**合并语义**，删不掉变量）
- 本地 `.dev.vars` 里的密钥与线上一致，无需改动
